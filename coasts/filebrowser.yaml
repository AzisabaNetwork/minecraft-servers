apiVersion: apps/v1
kind: Deployment
metadata:
  name: filebrowser-coasts
  namespace: minecraft
spec:
  selector:
    matchLabels:
      app: filebrowser-coasts
  replicas: 1
  revisionHistoryLimit: 2
  template:
    metadata:
      labels:
        app: filebrowser-coasts
    spec:
      containers:
      - name: filebrowser-coasts
        image: filebrowser/filebrowser
        volumeMounts:
        - mountPath: /database
          name: filebrowser-database
        - mountPath: /.filebrowser.json
          name: filebrowser-config
          subPath: ".filebrowser.json"
        - mountPath: /srv/coasts
          name: coasts-data
        resources:
          requests:
            cpu: "50m"
            memory: "100Mi"
          limits:
            cpu: "100m"
            memory: "500Mi"
        ports:
        - containerPort: 80
      volumes:
      - name: filebrowser-database
        persistentVolumeClaim:
          claimName: filebrowser-coasts-database
      - name: filebrowser-config
        configMap:
          name: filebrowser-coasts-config
      - name: coasts-data
        persistentVolumeClaim:
          claimName: server-coasts-data-claim
      # priorityClassName: minecraft-other-server
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: filebrowser-coasts-config
data:
  .filebrowser.json: |
    {
      "port": 80,
      "baseURL": "",
      "address": "",
      "log": "stdout",
      "database": "/database/filebrowser.db",
      "root": "/srv"
    }
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: filebrowser-coasts-database
spec:
  storageClassName: nfs-client
  accessModes:
  - "ReadWriteMany"
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: filebrowser-coasts-svc
spec:
  type: ClusterIP
  selector:
    app: filebrowser-coasts
  ports:
    - port: 80
      targetPort: 80
